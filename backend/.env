# Server Configuration
PORT=5000

# Database Configuration
DB_HOST=localhost
DB_USER=root
DB_PASS= root      # leave empty if your MySQL has no password
DB_NAME=read_for_change_db

# JWT Secret Key
JWT_SECRET=supersecretkey123   # choose any strong secret string

# CORS (optional - only if your frontend runs on a different port)
CLIENT_URL=http://localhost:3000
